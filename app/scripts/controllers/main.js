'use strict';

/**
 * @ngdoc function
 * @name mytodoApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the mytodoApp
 */
angular.module('SysdatApp')
  .controller('MainCtrl', function ($scope,$cookieStore, $location) {
    $scope.connectedUsr = {name:"", isConnected: ''}

    if

  });
